dim(available.packages()) #패키지 갯수 확인 코드
sessionInfo() # R프로그램의 버전, 운영체제 정보, 다국어 지원 현황 확인
install.packages("패키지명") #패키지 다운
remove.packages("패키지명") # 패키지 제거
install.packages("stringr") # 패키지 설치
installed.packages() #설치된 패키지 확인
library(stringr) #다운된 패키지 로드 코드
search() #현재 로드된 패키지 확인
